<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.user.mapper.UserNotificationMapper">

    <insert id="addUserNotification" useGeneratedKeys="true" keyColumn="id" parameterType="com.user.pojo.UserNotification">
        INSERT INTO
            user_notifications (user_id, message, is_read, type)
        VALUES
            (#{userNotification.userId}, #{userNotification.message}, #{userNotification.isRead}, #{userNotification.type})
    </insert>
    <update id="updateUserNotificationStatus">
        UPDATE user_notifications
        SET is_read = #{isRead}
        WHERE id = #{id}
    </update>
    <update id="updateUserNotificationsStatus">
        UPDATE user_notifications
        SET is_read = #{isRead}
        WHERE user_id = #{userId}
    </update>
    <select id="selectUserNotificationsByUserId" resultType="com.user.pojo.UserNotification">
        SELECT id, message, is_read, type
        FROM user_notifications
        WHERE user_id = #{userId}
    </select>

</mapper>