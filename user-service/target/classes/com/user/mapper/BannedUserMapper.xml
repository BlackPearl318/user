<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<mapper namespace="com.user.mapper.BannedUserMapper">

    <insert id="insertBannedUser" useGeneratedKeys="true" parameterType="com.user.pojo.BannedUser" keyProperty="id" keyColumn="id">
        INSERT INTO banned_users (user_id, ban_reason, ban_start_time, ban_end_time)
        VALUES (#{bannedUser.userId}, #{bannedUser.banReason}, #{bannedUser.banStartTime}, #{bannedUser.banEndTime})
    </insert>


</mapper>